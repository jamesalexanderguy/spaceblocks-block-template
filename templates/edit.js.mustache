import { useBlockProps } from '@wordpress/block-editor';
import { {{pascalCaseTitle}}Content } from './components';

export default function Edit() {
	return (
		<div {...useBlockProps()}>
			<{{pascalCaseTitle}}Content>
				{{title}} – hello from the editor!
			</{{pascalCaseTitle}}Content>
		</div>
	);
}
